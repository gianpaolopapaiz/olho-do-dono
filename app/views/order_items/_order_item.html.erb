<tr>
  <td><%= order_item.table.number %></td>
  <td><%= order_item.product.name %></td>
  <td><%= order_item.product_quantity %></td>
  <td><%= "#{order_item.created_at.hour-3}:#{order_item.created_at.min}" %></td>
  <% if order_item.status == 'placed'  %>
    <td><%= link_to 'prepare', order_item_path(order_item), method: 'patch', class: 'btn btn-outline-warning btn-sm' %></td>
    <td><%= link_to 'ready', '#', class: 'btn btn-outline-success btn-sm' %></td>
  <% elsif order_item.status == 'preparing' %>
    <td><%= link_to 'preparing', '#', class: 'btn btn-outline-warning btn-sm' %></td>
    <td><%= link_to 'ready', order_item_path(order_item), method: 'patch', class: 'btn btn-outline-success btn-sm' %></td> 
  <% end %>
  
</tr>
